import React from 'react'

type Props = {}

function NavBar({ }: Props) {
    return (
        <div className="navbar bg-base-100 shadow-sm">
            <a className="btn btn-ghost text-xl">AI Bots</a>
        </div>
    )
}

export default NavBar